<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DetailText">
        <item name="android:textColor">@color/colorLight</item>
        <item name="fontFamily">@font/gothic</item>
        <item name="android:layout_gravity">center_vertical</item>
    </style>
    <style name="GrayDetailText">
        <item name="android:textColor">@color/colorLightGray</item>
        <item name="fontFamily">@font/gothic</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="DetailButton">
        <item name="android:layout_height">20dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@null</item>
        <item name="android:textColor">@color/colorLight</item>
        <item name="fontFamily">@font/gothic</item>
    </style>

    <style name="BigAccentText">
        <item name="fontFamily">@font/gothic</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textAlignment">center</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">30sp</item>
    </style>

    <style name="CoordinateText">
        <item name="android:textAlignment">center</item>
        <item name="android:gravity">center</item>
        <item name="fontFamily">@font/gothic</item>
        <item name="android:textColor">@color/colorTintGray</item>
        <item name="android:textSize">50sp</item>
    </style>
</resources>